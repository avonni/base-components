import { LightningElement } from 'lwc';

export default class ColorPickerLabelInlineNoInput extends LightningElement {
    fieldLevelHelp =
        'The variant is "label-inline" and the color input is hidden';
}
