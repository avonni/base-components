import { LightningElement } from 'lwc';

export default class ColorPickerInlineNoInput extends LightningElement {
    fieldLevelHelp =
        'The variant is "label-inline" and the color input is hidden';
}
