/**
 * @typedef {Object} DateTimePickerAvatar
 * @name avatar
 * @property {string} fallbackIconName The Lightning Design System name of the icon used as a fallback when the option avatar image fails to load. Specify the name in the format 'utility:user' where 'utility' is the category, and 'user' is the specific icon to be displayed.
 * @property {string} initials If the record name contains two words, like first and last name, use the first capitalized letter of each. For records that only have a single word name, use the first two letters of that word using one capital and one lower case letter.
 * @property {string} size Size of the avatar. Valid values include x-small, small, medium, large, x-large and xx-large. Defaults to medium.
 * @property {string} src Image URL for the avatar.
 * @property {string} variant The variant changes the shape of the avatar. Accepted variants include circle and square. Defaults to square.
 */

/**
 * @namespace stylingHooks
 */
/**
 * @memberof stylingHooks
 * @name --avonni-date-time-picker-label-text-color
 * @default #444444
 * @type color
 */
/**
 * @memberof stylingHooks
 * @name --avonni-date-time-picker-label-font-size
 * @default 0.75rem
 * @type font
 */
/**
 * @memberof stylingHooks
 * @name --avonni-date-time-picker-label-font-style
 * @default normal
 * @type font
 */
/**
 * @memberof stylingHooks
 * @name --avonni-date-time-picker-label-font-weight
 * @default 700
 * @type font
 */
