import { LightningElement } from 'lwc';

export default class Button extends LightningElement {
    variantAttributes = {
        hasIcon: true,
        iconPosition: 'left',
        variant: 'base'
    };
}
